# Frontend

The participant and operator web interface for the platform shell. Use the
repository-level README for setup, security boundaries and release guidance.

For local development:

```bash
pnpm install --frozen-lockfile
pnpm dev
```
